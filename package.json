{
  "name": "kombu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "make",
    "check": "tsc --noEmit",
    "format": "prettier --write --no-semi .",
    "preview": "vite preview",
    "test": "make prebuilt-wasm && vitest run",
    "test:watch": "make prebuilt-wasm && vitest"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.11",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^0.34.5"
  },
  "dependencies": {
    "@wasmgroundup/emit": "^0.2.14"
  }
}
